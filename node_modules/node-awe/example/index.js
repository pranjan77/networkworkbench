
var AWE = require('../lib');
var fs = require('fs');

var url = 'http://140.221.84.236:8001/job';
var file = __dirname + '/test2';
var id = null;

var submitJob = function() {

	var awe = new AWE( url, file);
	awe.submitJob(function(err, result) {
		if (err) {
			console.log(err);
		} else {
			console.dir(result);

		}
	});
};

var getStatus = function(id) {
	var awe2 = new AWE( url );
	var id = 'ca21bfff-a8f2-4d32-932f-4b50e8c561b6';
	awe2.getStatus(id, function(error, attrib) {
		if (error) {
			console.dir(error);
		} else {
			console.dir(attrib);
			console.log('-------------');
			console.dir(attrib.data.tasks);
		}

	});
};

//setTimeout( submitJob, 600 );
setTimeout( getStatus, 600 );
